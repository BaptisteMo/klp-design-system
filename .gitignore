node_modules
dist
.DS_Store
*.log
.vite
coverage
.worktrees

# Agent-local artefacts
# The repo is a distribution surface: it ships components + the minimum needed
# to run the DS in an external project. Local tooling, scratch planning and
# failed captures never belong here.
.klp/staging
.klp/token-gaps.md
.klp/figma-refs/**/*.FAILED
docs/.lint-report.md
.agents/
plans/
skills-lock.json
# .claude/ is partly tracked on purpose (agents, commands, the klp-* pipeline
# skills). Only third-party / personal skills are ignored.
.claude/skills/improve

# env
.env.local
.env*.local
