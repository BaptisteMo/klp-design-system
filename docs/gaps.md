---
title: Design System Gaps
type: aggregation
updated: 2026-04-17
---

# Design System Gaps

Aggregated from all component `## Gaps` sections. Regenerated by the `documentalist` on every DOCUMENT and SYNC pass.

## Summary

| Component | Gap count |
|-----------|-----------|
| [ActionSheet Menu](./components/_index_action-sheet-menu.md) | 1 |
| [List](./components/_index_list.md) | 1 |
| [Table Cells / Actions](./components/_index_table-cells-actions.md) | 1 |
| [Table Cells / Checkbox](./components/_index_table-cells-checkbox.md) | 4 |
| [Table Cells / Empty](./components/_index_table-cells-empty.md) | 4 |
| [Table Cells / Text](./components/_index_table-cells-text.md) | 3 |

**Total gaps: 14 across 6 components**

## All gaps

| Component | Kind | Part | Property | Reason | Resolution |
|-----------|------|------|----------|--------|------------|
| [ActionSheet Menu](./components/_index_action-sheet-menu.md) | `unmatched-instance` | `separator` | — | No klp Separator component registered. Figma node 282:849 (`Separator` component set) has no corresponding klp entry in `klp-components.json`. | `inlined-local` — rendered as `<hr className="border-t border-klp-border-default">` (default/flat) or `<div className="h-[1px] bg-klp-border-default">` (checkbox). Promote to a standalone `separator` component when the Separator Figma spec is extracted. |
| [List](./components/_index_list.md) | `partial-reuse` | `header-title` | `color` | `header-title` color is a literal `#2b2b2b`; no `--klp-fg-*` alias in any brand variant maps to this value. Closest alias (`--klp-fg-default`) resolves to `--klp-color-gray-800` (wireframe), a different value. | `arbitrary-value-tailwind` — `text-[#2b2b2b]` applied via cva class string. Track as tech-debt: request a `--klp-fg-heading` alias from the design token layer, or confirm `fg/default` should be used and update Figma to match. |
| [Table Cells / Actions](./components/_index_table-cells-actions.md) | `unbound-literal` | `root` | `minHeight` | `minHeight` values 60px (height=2) and 52px (height=1) are bound to a cross-file Figma variable (`VariableID:44254ea5bdcf2155e180be9f7f5798c986ae6ae2/1050:1645`) from an external library — name unresolvable by the plugin. No matching `--klp-size-*` alias exists. | `min-h-[60px]` / `min-h-[52px]` CSS literals in the adapter. Request `--klp-size-row-relaxed` (60px) and `--klp-size-row-compact` (52px) aliases, or publish the external Figma library variable to the local token file and re-run `pnpm sync:tokens`. Note: Table Cells / Text has the same external variable for its own minHeight (44px/36px) — consistent cross-table gap. |
| [Table Cells / Empty](./components/_index_table-cells-empty.md) | `unbound-literal` | `root` | `minHeight` | `minHeight` 36px is bound to external primitive Figma variable (`VariableID:1628ef2701274f14d27cbdbfc02f93663d1cc555/1050:1644`, raw 36px). No `--klp-size-*` alias exists for 36px. Same variable seen in Table Cells / Text height=1. | `min-h-[36px]` CSS literal. Fix: add `--klp-size-row-sm` (36px) alias to the alias spacing layer and re-run `pnpm sync:tokens`. Resolves this gap across all table-cells components simultaneously. |
| [Table Cells / Empty](./components/_index_table-cells-empty.md) | `unbound-literal` | `root` | `paddingLeft` / `paddingRight` | `paddingLeft=12px` and `paddingRight=12px` are unbound Figma literals. Value matches `Sizing/S` = 12px = `--klp-size-s`. | `pl-[12px] pr-[12px]` CSS literals. Fix: bind to `Sizing/S` variable in Figma and re-run pipeline to replace with `pl-klp-size-s pr-klp-size-s`. |
| [Table Cells / Empty](./components/_index_table-cells-empty.md) | `unbound-literal` | `root` | `paddingTop` / `paddingBottom` (height=1) | `paddingTop=8px` and `paddingBottom=8px` are unbound Figma literals in height=1 variants. Value matches `Sizing/XS` = 8px = `--klp-size-xs`. | `pt-[8px] pb-[8px]` CSS literals. Fix: bind to `Sizing/XS` variable in Figma and re-run pipeline to replace with `pt-klp-size-xs pb-klp-size-xs`. |
| [Table Cells / Empty](./components/_index_table-cells-empty.md) | `unbound-literal` | `root` | `paddingTop` / `paddingBottom` (height=2) | `paddingTop=12px` and `paddingBottom=12px` are unbound Figma literals in the height=2 variant. Value matches `Sizing/S` = 12px = `--klp-size-s`. | `pt-[12px] pb-[12px]` CSS literals. Fix: bind to `Sizing/S` variable in Figma and re-run pipeline to replace with `pt-klp-size-s pb-klp-size-s`. |
| [Table Cells / Checkbox](./components/_index_table-cells-checkbox.md) | `unbound-literal` | `root` | `paddingLeft` / `paddingRight` | `paddingLeft=12px` and `paddingRight=12px` are unbound Figma literals (horizontal padding). Value matches `Sizing/S` = 12px = `--klp-size-s`. Same pattern as Table Cells / Empty. | `pl-[12px] pr-[12px]` CSS literals. Fix: bind to `Sizing/S` variable in Figma, re-run pipeline to replace with `pl-klp-size-s pr-klp-size-s`. |
| [Table Cells / Checkbox](./components/_index_table-cells-checkbox.md) | `unbound-literal` | `root` | `paddingTop` / `paddingBottom` (height=1) | `paddingTop=8px` / `paddingBottom=8px` are unbound Figma literals in the height=1 variant. Value matches `Sizing/XS` = 8px = `--klp-size-xs`. | `pt-[8px] pb-[8px]` CSS literals. Fix: bind to `Sizing/XS` variable in Figma (height=1 variants only), re-run pipeline. |
| [Table Cells / Checkbox](./components/_index_table-cells-checkbox.md) | `unbound-literal` | `root` | `paddingTop` / `paddingBottom` (height=2) | `paddingTop=12px` / `paddingBottom=12px` are unbound Figma literals in the height=2 variant. Value matches `Sizing/S` = 12px = `--klp-size-s`. | `pt-[12px] pb-[12px]` CSS literals. Fix: bind to `Sizing/S` variable in Figma (height=2 variants only), re-run pipeline. |
| [Table Cells / Checkbox](./components/_index_table-cells-checkbox.md) | `unbound-literal` | `root` | `minHeight` | `minHeight` 36px is bound to an external cross-file Figma variable (`VariableID:1628ef2701274f14d27cbdbfc02f93663d1cc555/1050:1644`, raw 36px). No `--klp-size-*` alias exists for 36px. Same variable seen in Table Cells / Empty and Table Cells / Text height=1. | `min-h-[36px]` CSS literal. Fix: add `--klp-size-row-sm` (36px) alias to the alias spacing layer and re-run `pnpm sync:tokens`. Resolves this gap across all three table-cells components simultaneously. |
| [Table Cells / Text](./components/_index_table-cells-text.md) | `unbound-literal` | `root` | `minHeight` (height=2) | `minHeight` 44px for `height=2` variants is bound to an external primitive Figma variable (`VariableID:44254ea5.../1050:1645`, raw 44px). No matching `--klp-size-*` alias exists in the klp token system. | `min-h-[44px]` CSS literal in the adapter. Request a `--klp-size-row-relaxed` (or equivalent) alias from the token layer. |
| [Table Cells / Text](./components/_index_table-cells-text.md) | `unbound-literal` | `root` | `minHeight` (height=1) | `minHeight` 36px for `height=1` variants is bound to an external primitive Figma variable (`VariableID:1628ef27.../1050:1644`, raw 36px). No matching `--klp-size-*` alias exists in the klp token system. | `min-h-[36px]` CSS literal in the adapter. Request a `--klp-size-row-compact` (or equivalent) alias from the token layer. |
| [Table Cells / Text](./components/_index_table-cells-text.md) | `unbound-literal` | `root` | `paddingLeft` / `paddingRight` | `paddingLeft` and `paddingRight` (8px) are not bound to any Figma variable — literal in the spec. The resolved value matches `Sizing/XS` = 8px = `--klp-size-xs`. | `pl-[8px] pr-[8px]` CSS literals in the adapter. Fix in Figma: bind these properties to the `Sizing/XS` variable, then re-run the pipeline to replace with `pl-klp-size-xs pr-klp-size-xs`. |
