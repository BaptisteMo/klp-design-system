# Documentalist log

Append-only. Never delete entries.

## [2026-08-04] SYNC | action-sheet-menu, tabulations, sidebar — Separator-adoption pass, 3 components, 2 new forward edges
Resynced three components after a Separator-adoption refactor (source changed, spec unchanged). **action-sheet-menu**: local `separatorVariants` cva and inlined `<hr>` deleted; `ActionSheetSeparator` now renders `<Separator margin={type === 'checkbox' ? 'none' : 'medium'} />` (default/flat → `margin="medium"` matching Figma's `paddingY: --klp-size-m`; checkbox → `margin="none"` matching the former bare 1px rectangle — verified in the browser: 33px vs 1px wrapper height, identical to prior rendering). This resolves its previously-recorded `unmatched-instance`/`separator` composition gap. `.klp/figma-refs/action-sheet-menu/spec.json` was backfilled with a `composition.reuses: [action-sheet-item, checkbox, separator]` block (v1 spec predating the composition discipline) — 0 `undeclared-reuse` warnings now (were 2, for action-sheet-item and checkbox). Updated `klp-components.json[action-sheet-menu]`: `dependencies.components: [action-sheet-item, checkbox, separator]`, `composition.reuses` gained the separator entry, `composition.gaps: []`. Synced `registry/action-sheet-menu.json#meta.composition` (same). Updated doc: Anatomy separator line, klp-components Dependencies list, `KLP:GAPS` block now reads "No gaps recorded." **tabulations**: local `dividerVariants` cva deleted; the vertical rule between tabs is now `<Separator direction="vertical" margin="none" className="h-[24px] self-center" />` — first DS consumer of Separator's vertical axis (verified in the browser: 5 dividers, 1×24px, `data-orientation="vertical"`, `#D7DAD9`). `.klp/figma-refs/tabulations/spec.json` backfilled with `composition.reuses: [separator, tabulation-cells]` (same v1-spec reason) — 0 warnings now. Updated `klp-components.json[tabulations].dependencies.components: [separator, tabulation-cells]`; synced `registry/tabulations.json#meta.dependencies`. Updated doc: Anatomy divider line, klp-components Dependencies list. **sidebar**: cosmetic-only fix (duplicate `border-t` in the footer cva class string removed; final rendered class unchanged, no visual/API diff) — doc `updated` date refreshed, body otherwise unchanged. `sidebar` still carries one pre-existing `undeclared-reuse` warning for `item-side-bar` (v1 spec, untouched by this refactor — left as-is per instruction, not fabricating a composition block for a relationship not actually touched). Reverse-index pass (single pass covering all edges above): 2 new forward edges (action-sheet-menu→separator, tabulations→separator). Patched `separator` frontmatter `usedBy: [action-sheet-menu, calendar, tabulations]` and `## Used by` section; patched `klp-components.json[separator].usedBy` to match. Manual note added inside `docs/components/_index_modal-variation.md`'s `KLP:NOTES` block (untouched by the refactor itself, per explicit instruction): the footer's top border resolves to `--klp-bg-inset`, a `bg-*`-named token used as a border color — flagged as a design-side token-naming smell to raise with the design team, source left unchanged. Regenerated `docs/gaps.md`: removed the action-sheet-menu row (total gaps 15 → 14, components-with-gaps 8 → 7); `action-sheet-menu` no longer appears in the report (`tabulations` never carried a typed gap, so it doesn't change the count). klp-doc-rules-validator run on all three components individually: passed=true, autoFixed=[], mismatches=[], warnings=[] for each.

## [2026-04-23] DOCUMENT | modal-variation — 2 variants, 6 dependencies (1 component, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_modal-variation.md`. Updated `klp-components.json` canonical entry (confirmed props with raw TS types from source interface; corrected `registry/modal-variation.json#dependencies.npm` — removed stale `lucide-react`, component source does not import it). Reverse-index pass: patched `button` usedBy to add `modal-variation` (both frontmatter and `## Used by` section). Patched token group pages: `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md`. Patched `brands/wireframe.md`. Added entry under `overlays` in `docs/index.md`. Regenerated `docs/gaps.md` (2 new modal-variation gaps: literal-gap on footer fill, token-gap on root cornerRadius; total 13 gaps across 7 components).

## [2026-04-22] DOCUMENT | sidebar — 2 variants, 6 dependencies (2 components, 4 token groups, 1 brand), 0 usedBy
Regenerated `docs/components/_index_sidebar.md` after refactor from consumer-configurable `menuItems` API to baked-in `SIDEBAR_MENU` tree. Removed stale `menuItems` / `SideBarMenuItem` from props; added `activeKey` (persistent), `activeChildKey` (persistent), `onNavigate` (optional). Updated `klp-components.json` props block accordingly. Added `menu.ts` to `registry/sidebar.json#files`. Externals corrected: removed transitive `@radix-ui/react-slot` (direct imports are `class-variance-authority` and `lucide-react` only). Reverse-index pass: 0 new edges (sidebar is a leaf in usedBy graph).

## [2026-04-16] BOOTSTRAP | docs tree initialized
Created `docs/`, `docs/components/`, `docs/tokens/`, `docs/brands/` with skeletons. 4 brand pages seeded from `aliases.css` (wireframe, klub, atlas, showup). 4 token-group pages seeded from `theme.css` (colors, spacing, radius, typography).

## [2026-04-16] DOCUMENT | button — 20 variants, 4 dependencies (0 components, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_button.md`. Updated `klp-components.json` to canonical schema (added `doc` field, set `captureBrand: atlas`, populated `dependencies` and `variantCount`). Reverse-index pass: 1 component scanned, no inbound edges (Button is a leaf).

## [2026-04-16] DOCUMENT | checkbox — 5 variants, 3 dependencies (0 components, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_checkbox.md`. Updated `klp-components.json` canonical entry (corrected `captureBrand` from `wireframe` to `klub` per spec.json). Reverse-index pass: 2 components scanned, 0 component-to-component edges. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md` Used-by sections to add Checkbox. Patched `brands/klub.md` Used-by section (first component validated under klub).

## [2026-04-16] DOCUMENT | radio — 4 variants, 2 dependencies (0 components, 2 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_radio.md`. Updated `klp-components.json` canonical entry (confirmed `captureBrand: klub`, `tokenGroups: [colors, spacing]`). Reverse-index pass: 3 components scanned, 0 component-to-component edges. Patched `tokens/colors.md` and `tokens/spacing.md` Used-by sections to add Radio. Patched `brands/klub.md` Used-by section to add Radio.

## [2026-04-16] DOCUMENT | switch — 2 variants, 3 dependencies (0 components, 2 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_switch.md`. Updated `klp-components.json` canonical entry (corrected `externals` to include `lucide-react`, confirmed `captureBrand: wireframe`, `tokenGroups: [colors, spacing]`). Reverse-index pass: 4 components scanned, 0 component-to-component edges. Patched `tokens/colors.md` and `tokens/spacing.md` Used-by sections to add Switch. Patched `brands/wireframe.md` Used-by section (first component validated under wireframe).

## [2026-04-16] DOCUMENT | tooltip — 4 variants, 4 dependencies (0 components, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_tooltip.md`. Updated `klp-components.json` canonical entry (confirmed `captureBrand: klub`, `tokenGroups: [colors, radius, spacing, typography]`, `externals: [@radix-ui/react-tooltip, class-variance-authority]`). Updated `docs/index.md` to add tooltip under `overlay` category. Reverse-index pass: 5 components scanned, 0 component-to-component edges. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md` Used-by sections to add Tooltip. Patched `brands/klub.md` Used-by section to add Tooltip.

## [2026-04-16] DOCUMENT | badges — 48 variants, 3 dependencies (0 components, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_badges.md`. Updated `klp-components.json` canonical entry (corrected `source` from `Badge.tsx` to `Badges.tsx`; removed `radius` from `tokenGroups` as badge uses literal `4px` cornerRadius — no `--klp-radius-*` token consumed). Updated `docs/index.md` to add Badge under `data-display` category. Reverse-index pass: 6 components scanned, 0 component-to-component edges. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add Badge. Patched `brands/wireframe.md` Used-by section to add Badge.

## [2026-04-16] DOCUMENT | input — 18 variants, 4 dependencies (0 components, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_input.md`. Updated `klp-components.json` canonical entry (corrected `externals` from `[@radix-ui/react-slot, class-variance-authority]` to `[class-variance-authority, lucide-react]` — source imports `lucide-react` for the Info icon, not `@radix-ui/react-slot`). Updated `docs/index.md` to add Input under `inputs` category. Reverse-index pass: 7 components scanned, 0 component-to-component edges. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md` Used-by sections to add Input. Patched `brands/klub.md` Used-by section to add Input.

## [2026-04-17] DOCUMENT | list-content — 9 variants, 4 dependencies (0 components, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_list-content.md`. Updated `klp-components.json` canonical entry (stub already had correct fields; added `created`/`updated` dates). Updated `docs/index.md` to add `list` category section with List Content. Reverse-index pass: 8 components scanned, 0 component-to-component edges. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md` Used-by sections to add List Content. Patched `brands/wireframe.md` Used-by section to add List Content.

## [2026-04-17] DOCUMENT | table-row — 5 variants, 4 dependencies (0 components, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_table-row.md`. Updated `klp-components.json` canonical entry (corrected `tokenGroups` sort order to alphabetical; all other fields already correct in stub). Updated `docs/index.md` to add Table Row under `data-display` category. Reverse-index pass: 9 components scanned, 0 component-to-component edges. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md` Used-by sections to add Table Row. Patched `brands/klub.md` Used-by section to add Table Row. One token gap flagged: empty-message text fill hardcoded black in Figma — adapter resolves to `--klp-fg-default`.

## [2026-04-17] DOCUMENT | action-sheet-item — 21 variants, 3 dependencies (0 components, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_action-sheet-item.md`. Confirmed `klp-components.json` entry already canonical (stub had correct fields from component-adapter). Updated `docs/index.md` to add `overlays` category section with ActionSheet Item. Reverse-index pass: 10 components scanned, 0 component-to-component edges (all leaves). Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add ActionSheet Item. Patched `brands/wireframe.md` Used-by section to add ActionSheet Item. One UNVERIFIED note: cornerRadius bound to cross-file Figma variable IDs — recorded as literals 8px/4px per spec.json:_notes.cornerRadiusGap.

## [2026-04-17] DOCUMENT | floating-alert — 12 variants, 4 dependencies (0 components, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_floating-alert.md`. Updated `klp-components.json` canonical entry (corrected `tokenGroups` sort order to alphabetical: colors/radius/spacing/typography; all other fields already correct in stub). Updated `docs/index.md` to add Floating Alert under `feedback` category (previously empty). Reverse-index pass: 11 components scanned, 0 component-to-component edges (all leaves). Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md` Used-by sections to add Floating Alert. Patched `brands/wireframe.md` Used-by section to add Floating Alert. Three token gaps flagged in spec.json:tokenGaps (cornerRadius bound to unresolved Figma collection 293; adapter uses literal `rounded-[8px]`; closest alias `--klp-radius-m`).

## [2026-04-17] DOCUMENT | in-content-alert — 12 variants, 3 dependencies (0 components, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_in-content-alert.md`. `klp-components.json` stub already canonical from component-adapter (all fields correct). Updated `docs/index.md` to add InContent Alert under `feedback` category. Reverse-index pass: 12 components scanned, 0 component-to-component edges (all leaves). Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add InContent Alert. Patched `brands/klub.md` Used-by section to add InContent Alert. No token gaps. Leaf component — no `radius` group (corner radius uses `--klp-size-*` tokens or literals, not `--klp-radius-*`).

## [2026-04-17] DOCUMENT | breadcrumbs — 4 variants, 2 dependencies (0 components, 2 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_breadcrumbs.md`. Updated `klp-components.json` canonical entry (corrected `externals` — removed `@radix-ui/react-slot` from list as source does not import it directly; confirmed `tokenGroups: [colors, typography]`). Updated `docs/index.md` to populate `navigation` category (previously empty). Reverse-index pass: 13 components scanned, 0 component-to-component edges (all leaves). Patched `tokens/colors.md` and `tokens/typography.md` Used-by sections to add BreadCrumbs. Patched `brands/wireframe.md` Used-by section to add BreadCrumbs. Two token gaps flagged in spec.json:tokenGaps (iconSize 16px and wrapperSize 20px have no `--klp-size-icon-*` alias — recorded as literals).

## [2026-04-17] DOCUMENT | tabulation-cells — 2 variants, 3 dependencies (0 components, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_tabulation-cells.md`. Updated `klp-components.json` canonical entry (`doc` field set from null to canonical path; all other fields already correct in stub). Updated `docs/index.md` to add Tabulation Cells under `navigation` category. Reverse-index pass: 14 components scanned, 0 component-to-component edges (all leaves). Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add Tabulation Cells. Patched `brands/klub.md` Used-by section to add Tabulation Cells. Five token gaps flagged in spec.json:tokenGaps (root cornerRadius 8px, paddingLeft/Right 12px, paddingTop/Bottom 6px, itemSpacing 8px, badge cornerRadius 4px all use CSS literals pending Figma token assignment for `--klp-radius-m`, `--klp-size-s`, `--klp-size-2xs`, `--klp-size-xs`).

## [2026-04-17] DOCUMENT | tabulations — 1 variant, 4 dependencies (1 component, 2 externals, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_tabulations.md`. Updated `klp-components.json` canonical entry (description expanded; all other fields already correct from component-adapter stub). Updated `docs/index.md` to add Tabulations under `navigation` category. Reverse-index pass: 15 components scanned, 1 component-to-component edge (tabulations → tabulation-cells). Patched `tabulation-cells` frontmatter `usedBy: [tabulations]` and `## Used by` section. Patched `klp-components.json` `tabulation-cells.usedBy` to `["tabulations"]`. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md` Used-by sections to add Tabulations. Patched `brands/klub.md` Used-by section to add Tabulations. Ten token gaps flagged in spec.json:tokenGaps (root/cell-root cornerRadius, paddingAll, itemSpacing all use CSS literals — cross-file Figma library variables; candidates: `--klp-radius-l`, `--klp-size-4xs`, `--klp-size-3xs`, `--klp-size-s`, `--klp-size-2xs`, `--klp-size-xs`).

## [2026-04-17] DOCUMENT | text-area — 12 variants, 3 dependencies (0 components, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_text-area.md`. Updated `klp-components.json` canonical entry (stub from component-adapter corrected: description expanded, `externals` completed with `@radix-ui/react-slot`). Updated `docs/index.md` to add Text Area under `inputs` category. Reverse-index pass: 16 components scanned, 0 new component-to-component edges (text-area is a leaf). Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add Text Area. Patched `brands/wireframe.md` Used-by section to add Text Area. Two token gaps recorded from spec.json:tokenGaps: cornerRadius=8px hardcoded (should use `--klp-radius-l`) and info-icon size 20px has no `--klp-size-icon-md` alias yet.

## [2026-04-17] SYNC | one-shot migration — 16 components processed, 5 forward edges added, 3 usedBy arrays updated
Backfilled stale `dependencies.components[]` and `usedBy[]` in `klp-components.json` and all 16 component doc pages by re-scanning `@/components/*` imports across all source files. Forward edges added: floating-alert→button, list-content→button, tabulation-cells→badges, tabulations→tabulation-cells, text-area→button. Reverse edges updated: button.usedBy=[floating-alert,list-content,text-area], badges.usedBy=[tabulation-cells], tabulation-cells.usedBy=[tabulations]. KLP:GAPS blocks seeded on all 16 pages with empty fallback. Created `docs/gaps.md` aggregation page.

## [2026-04-17] DOCUMENT | action-sheet-menu — 3 variants, 6 dependencies (2 components, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_action-sheet-menu.md`. Updated `klp-components.json` canonical entry (normalised `tokenGroups` sort order, refined `composition.gaps` to use `kind/part/reason/action` schema). Updated `docs/index.md` to add `lists` category section with ActionSheet Menu. Reverse-index pass: 17 components scanned, 2 new forward edges (action-sheet-menu→action-sheet-item, action-sheet-menu→checkbox). Patched `action-sheet-item` and `checkbox` frontmatter `usedBy` and `## Used by` sections. Patched `klp-components.json` `action-sheet-item.usedBy=["action-sheet-menu"]` and `checkbox.usedBy=["action-sheet-menu"]`. 1 composition gap recorded: `unmatched-instance` on `separator` part (no klp Separator component, inlined-local). Regenerated `docs/gaps.md` — total gaps: 1.

## [2026-04-17] DOCUMENT | list — 3 variants, 5 dependencies (1 component, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_list.md`. Promoted `klp-components.json` entry from stub to canonical (status: stable, doc path set, gap kind corrected from `missing-token` to `partial-reuse`). Updated `docs/index.md` to add List under existing `lists` category section. Reverse-index pass: 18 components scanned, 1 new forward edge (list→list-content). Patched `list-content` frontmatter `usedBy: [list]` and `## Used by` section. Patched `klp-components.json` `list-content.usedBy=["list"]`. 1 composition gap recorded: `partial-reuse` on `header-title` color (literal `#2b2b2b` — no `--klp-fg-*` alias matches; arbitrary-value Tailwind applied). Regenerated `docs/gaps.md` — total gaps: 2 across 2 components.

## [2026-04-17] DOCUMENT | table-cells-text — 10 variants, 6 dependencies (3 components, 3 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_table-cells-text.md`. Updated `klp-components.json` canonical entry (sorted `dependencies.components` alphabetically: badges, button, checkbox). Updated `docs/index.md` to add `table cells` category section. Reverse-index pass: 19 components scanned, 3 new forward edges (table-cells-text→badges, table-cells-text→button, table-cells-text→checkbox). Patched `badges` frontmatter `usedBy: [tabulation-cells, table-cells-text]` and `## Used by` section. Patched `button` frontmatter `usedBy: [floating-alert, list, list-content, table-cells-text, text-area]` and `## Used by` section. Patched `checkbox` frontmatter `usedBy: [action-sheet-menu, table-cells-text]` and `## Used by` section. Patched `klp-components.json` usedBy arrays for badges, button, checkbox. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections. Patched `brands/wireframe.md` Used-by section. KLP:GAPS block seeded with 3 gaps: minHeight 44px (no alias), minHeight 36px (no alias), paddingLeft/Right literal 8px unbound in Figma (matches --klp-size-xs). Regenerated `docs/gaps.md` — total gaps: 5 across 3 components.

## [2026-04-17] DOCUMENT | list (re-run) — 3 variants, 6 dependencies (2 components, 4 token groups, 1 brand), 0 usedBy
Source updated: `header-button` now rendered as `<Button variant="tertiary" size="md">` from `@/components/button` (was a raw `<button>`). Re-scanned `@/components/*` imports — detected: `button`, `list-content`. Rewrote `dependencies.components: ["button", "list-content"]` in list frontmatter and `klp-components.json`. Reverse-index pass: added forward edge list→button; patched `button` frontmatter `usedBy: [floating-alert, list, list-content, text-area]` and `## Used by` section; patched `klp-components.json` `button.usedBy`. Updated list doc `## Dependencies` klp-components section to document the button import. Regenerated `docs/gaps.md` — list header-title color literal gap carried forward unchanged. Total gaps still 2 across 2 components.

## [2026-04-17] DOCUMENT | table-cells-badges — 22 variants, 3 dependencies (1 component, 2 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_table-cells-badges.md`. Confirmed `klp-components.json` stub was already canonical from component-adapter (added `composition.reuses/gaps` fields; all other fields already correct). Updated `docs/index.md` to add Table Cells / Badges under existing `table cells` category. Import scan: `@/components/badges` detected (expected `['badges']` — confirmed). Reverse-index pass: 20 components scanned, 1 new forward edge (table-cells-badges→badges). Patched `badges` frontmatter `usedBy: [table-cells-badges, tabulation-cells, table-cells-text]` and `## Used by` section. Patched `klp-components.json` `badges.usedBy`. Patched `tokens/colors.md` and `tokens/spacing.md` Used-by sections to add Table Cells / Badges. Patched `brands/klub.md` Used-by section to add Table Cells / Badges. KLP:GAPS block: `composition.gaps=[]` → rendered "No gaps recorded." `docs/gaps.md` re-confirmed: 5 gaps across 3 components (table-cells-badges contributes 0).

## [2026-04-17] DOCUMENT | table-cells-actions — 8 variants, 4 dependencies (1 component, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_table-cells-actions.md`. Updated `klp-components.json` canonical entry: promoted `tokenGroups` from `[colors, spacing]` to `[colors, radius, spacing, typography]` (radius consumed via `--klp-radius-l` on all button layers; typography consumed via `--klp-font-size-text-medium`, `--klp-font-family-label`, `--klp-font-weight-label-bold` on button-primary label); normalised `composition.gaps` from string array to structured object schema; corrected `description` to match spec.json. Updated `docs/index.md` to add Table Cells / Actions under existing `table cells` category section. Import scan: `@/components/button` detected (expected `['button']` — confirmed). Reverse-index pass: 21 components scanned, 1 new forward edge (table-cells-actions→button). Patched `button` frontmatter `usedBy: [floating-alert, list, list-content, table-cells-actions, table-cells-text, text-area]` and `## Used by` section. Patched `klp-components.json` `button.usedBy`. Patched `tokens/colors.md`, `tokens/radius.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add Table Cells / Actions. Patched `brands/wireframe.md` Used-by section to add Table Cells / Actions. KLP:GAPS block seeded with 1 gap: `root/minHeight` 60px (height=2) / 52px (height=1) — cross-file Figma variable, treated as literals `min-h-[60px]` / `min-h-[52px]`. Regenerated `docs/gaps.md` — total gaps: 6 across 4 components.

## [2026-04-17] DOCUMENT | table-cells-empty — 3 variants, 2 dependencies (0 components, 2 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_table-cells-empty.md`. Updated `klp-components.json` canonical entry (promoted `radixPrimitive` from `null` to `"none"`; all other fields already correct from component-adapter stub). Updated `docs/index.md` to add Table Cells / Empty under existing `table cells` category. Import scan: no `@/components/*` imports detected — confirmed leaf (expected: []). Reverse-index pass: 22 components scanned, 0 new component-to-component edges (table-cells-empty is a leaf). Patched `tokens/colors.md` and `tokens/spacing.md` Used-by sections to add Table Cells / Empty. Patched `brands/wireframe.md` Used-by section to add Table Cells / Empty. KLP:GAPS block seeded with 4 literal gaps: (1) minHeight 36px — no `--klp-size-*` alias (same VariableID as table-cells-text height=1); (2) paddingLeft/Right 12px unbound (matches `--klp-size-s`); (3) paddingTop/Bottom 8px unbound height=1 (matches `--klp-size-xs`); (4) paddingTop/Bottom 12px unbound height=2 (matches `--klp-size-s`). Regenerated `docs/gaps.md` — total gaps: 10 across 5 components.

## [2026-04-17] DOCUMENT | table-cells-checkbox — 3 variants, 3 dependencies (1 component, 2 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_table-cells-checkbox.md`. Promoted `klp-components.json` entry from stub to canonical (`status: stub` → `stable`; added `doc` field; added `usedBy: []`; all other fields already correct from component-adapter stub). Updated `docs/index.md` to add Table Cells / Checkbox under existing `table cells` category. Import scan: `@/components/checkbox` detected — confirmed `['checkbox']` (matches spec.json:composition.reuses). Reverse-index pass: 23 components scanned, 1 new forward edge (table-cells-checkbox→checkbox). Patched `checkbox` frontmatter `usedBy: [action-sheet-menu, table-cells-checkbox, table-cells-text]` and `## Used by` section. Patched `klp-components.json` `checkbox.usedBy`. Patched `tokens/colors.md` and `tokens/spacing.md` Used-by sections to add Table Cells / Checkbox. Patched `brands/klub.md` Used-by section to add Table Cells / Checkbox. KLP:GAPS block seeded with 4 literal gaps (identical pattern to Table Cells / Empty): (1) paddingLeft/Right 12px unbound (expected `--klp-size-s`); (2) paddingTop/Bottom 8px unbound height=1 (expected `--klp-size-xs`); (3) paddingTop/Bottom 12px unbound height=2 (expected `--klp-size-s`); (4) minHeight 36px — same external cross-file Figma variable as table-cells-empty/table-cells-text height=1 (no `--klp-size-*` alias). Regenerated `docs/gaps.md` — total gaps: 14 across 6 components.

## [2026-04-20] DOCUMENT | item-side-bar — 6 variants, 5 dependencies (1 component, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_item-side-bar.md`. Updated `klp-components.json` canonical entry (promoted stub to canonical: corrected `tokenGroups` sort order alphabetically, `variantAxes` key order). Updated `docs/index.md` to add Item Side Bar under `navigation` category. Import scan: `@/components/action-sheet-item` detected — confirmed `dependencies.components: ["action-sheet-item"]`. Reverse-index pass: 1 new forward edge (item-side-bar → action-sheet-item). Patched `action-sheet-item` frontmatter `usedBy: [action-sheet-menu, item-side-bar]` and `## Used by` section. Patched `klp-components.json` `action-sheet-item.usedBy`. Patched `tokens/colors.md`, `tokens/radius.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add Item Side Bar. Patched `brands/klub.md` Used-by section to add Item Side Bar. 3 composition gaps recorded: `unmatched-instance` on `decorative-icon`, `icon`, and `chevron` parts (no klp component available for each — inlined-local-cva). Regenerated `docs/gaps.md` — total gaps: 5 across 3 components.

## [2026-04-20] DOCUMENT | header-desktop — 2 variants, 7 dependencies (3 components, 3 externals, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_header-desktop.md`. Updated `klp-components.json` canonical entry (promoted stub: corrected `tokenGroups` to include `radius`; sorted `dependencies.components` alphabetically to `[breadcrumbs, button, input]`). Updated `docs/index.md` to add Header Desktop under `navigation` category. Import scan: `@/components/breadcrumbs`, `@/components/button`, `@/components/input` detected — confirmed `dependencies.components: [breadcrumbs, button, input]`. Reverse-index pass: 3 new forward edges (header-desktop→breadcrumbs, header-desktop→button, header-desktop→input). Patched `breadcrumbs` frontmatter `usedBy: [header-desktop]` and `## Used by` section. Patched `button` frontmatter `usedBy: [floating-alert, header-desktop, list, list-content, pagination, text-area]` and `## Used by` section. Patched `input` frontmatter `usedBy: [header-desktop]` and `## Used by` section. Patched `klp-components.json` usedBy arrays for breadcrumbs, button, input. Patched `tokens/colors.md`, `tokens/radius.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections. Patched `brands/klub.md` Used-by section. 0 composition gaps (all 3 DS composition instances matched: button, input, breadcrumbs). Regenerated `docs/gaps.md` — total gaps unchanged: 5 across 3 components.

## [2026-04-17] SYNC | table refactor — 20 components processed, 5 new forward edges, 6 docs deleted, 3 docs created
Table refactor: 3 new components (table, pagination, data-table) + 6 deleted (table-row, table-cells-text, table-cells-badges, table-cells-actions, table-cells-empty, table-cells-checkbox). Deleted 6 stale doc files. Created docs/components/_index_table.md, _index_pagination.md, _index_data-table.md from source + klp-components.json stubs (spec: null). Import scan across all 20 active component sources. New forward edges: data-table→[pagination, table], pagination→[button]. Reverse edges updated: button.usedBy=[floating-alert, list, list-content, pagination, text-area] (table-cells-actions and table-cells-text removed; pagination added); badges.usedBy=[tabulation-cells] (table-cells-badges and table-cells-text removed); checkbox.usedBy=[action-sheet-menu] (table-cells-checkbox and table-cells-text removed); pagination.usedBy=[data-table]; table.usedBy=[data-table]. Regenerated docs/index.md, docs/gaps.md (14 gaps → 2 gaps). Updated tokens/colors.md, tokens/spacing.md, tokens/radius.md, tokens/typography.md Used-by sections. Updated brands/wireframe.md, brands/klub.md Used-by sections. Updated klp-components.json: all 20 entries have correct dependencies.components[] and usedBy[] arrays.

## [2026-04-20] DOCUMENT | sidebar — 2 variants, 6 dependencies (2 components, 3 externals, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_sidebar.md`. Updated `klp-components.json` canonical entry (corrected `tokenGroups` sort order to alphabetical: colors/radius/spacing/typography; `doc` field already present in stub). Added SideBar under `navigation` category in `docs/index.md`. Import scan: `@/components/button`, `@/components/item-side-bar` detected — confirmed `dependencies.components: ["button", "item-side-bar"]`. Reverse-index pass: 2 new forward edges (sidebar→button, sidebar→item-side-bar). Patched `button` frontmatter `usedBy: [floating-alert, header-desktop, header-phone, list, list-content, pagination, sidebar, text-area]` and `## Used by` section. Patched `item-side-bar` frontmatter `usedBy: [sidebar]` and `## Used by` section. Patched `klp-components.json` `button.usedBy` and `item-side-bar.usedBy`. Patched `tokens/colors.md`, `tokens/radius.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add SideBar. Patched `brands/klub.md` Used-by section to add SideBar. 2 composition gaps recorded: `unmatched-instance` on `logo` (no brand-logo component, inlined-ad-hoc slot), `new-primitive` on `avatar` (no DS Avatar component, inlined-ad-hoc). Regenerated `docs/gaps.md` — total gaps: 8 across 5 components.

## [2026-04-20] DOCUMENT | header-phone — 1 variant, 6 dependencies (2 components, 3 externals, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_header-phone.md`. Updated `klp-components.json` entry already canonical from component-adapter stub (confirmed all fields correct). Added `containers` category to `docs/index.md`. Import scan: `@/components/button`, `@/components/breadcrumbs` detected — confirmed `dependencies.components: [breadcrumbs, button]`. Reverse-index pass: 2 new forward edges (header-phone→breadcrumbs, header-phone→button). Patched `breadcrumbs` frontmatter `usedBy: [header-desktop, header-phone]` and `## Used by` section. Patched `button` frontmatter `usedBy: [floating-alert, header-desktop, header-phone, list, list-content, pagination, text-area]` and `## Used by` section. Patched `klp-components.json` `breadcrumbs.usedBy` and `button.usedBy`. Patched `tokens/colors.md`, `tokens/radius.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add Header Phone. Patched `brands/klub.md` Used-by section to add Header Phone. 1 composition gap recorded: `unmatched-instance` on `logo` part (no `brand-logo` component, inlined-ad-hoc slot). Regenerated `docs/gaps.md` — total gaps: 6 across 4 components.

## [2026-04-21] DOCUMENT | collapsible — 2 variants, 5 dependencies (1 component, 3 externals, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_collapsible.md`. `klp-components.json` entry already present from component-adapter stub (all canonical fields correct including `doc` path); confirmed and left unchanged. Added `disclosure` category section to `docs/index.md`. Import scan: `@/components/button` detected — confirmed `dependencies.components: ["button"]`. Reverse-index pass: 1 new forward edge (collapsible→button). Patched `button` frontmatter `usedBy: [collapsible, floating-alert, header-desktop, header-phone, list, list-content, pagination, sidebar, text-area]` and `## Used by` section. Patched `klp-components.json` `button.usedBy`. Patched `tokens/colors.md`, `tokens/radius.md`, `tokens/spacing.md`, `tokens/typography.md` Used-by sections to add Collapsible. Patched `brands/wireframe.md` Used-by section to add Collapsible. 1 composition gap recorded: `partial-reuse` on `toggle-button` part (Button correctly imported and used; ChevronDown icon rotation via className override — one-off visual tweak, not a structural deviation). Regenerated `docs/gaps.md` — total gaps: 9 across 6 components.

## [2026-04-21] SYNC | @propClass full pass — 25 components, 12 doc pages regenerated
Re-parsed `@propClass` JSDoc tags and `@derivedFrom` annotations across all 25 component source files. Rewrote `klp-components.json[<name>].props` for every component with shape `{ class, type, default, description, derivedFrom }`. Regenerated 25 `docs/components/_index_<name>.md` pages: replaced `## API` section with `## Props usage` table (added `Class` column). Emitted Do/Don't blocks for components with `computed` props: Input (state derived from disabled/aria-invalid/focus/value), TextArea (same). Emitted Do/Don't blocks for components with `persistent` props: ActionSheetItem (state owned by parent action sheet), ItemSideBar (state owned by router/page context), ListContent (state owned by parent list). Emitted Class B notes for: Button (state via CSS :hover/:active/[disabled]), Checkbox (state via Radix data-state/data-disabled), Switch (state via Radix data-state), Radio (state via Radix data-state/data-disabled), TabulationCells (state bridged via Radix data-state MutationObserver). Reverse-index pass: graph symmetric — forward edges unchanged from prior passes. Regenerated `docs/gaps.md` — total gaps: 10 across 6 components (SideBar gained 2 new token-gap entries for context-label/user-name literal font sizes detected during SYNC). Props missing @propClass: 0 (all props tagged). Warnings: 0.

## [2026-04-21] DOCUMENT | header-phone — 1 variant, 2 component dependencies (breadcrumbs, button), 0 usedBy
Regenerated `docs/components/_index_header-phone.md`. Corrected stale API section: previous doc listed `breadcrumbSteps` (wrong prop name) and was missing `showSearch` and `breadcrumbs: BreadCrumbStep[] | false`; replaced with actual `HeaderPhoneProps` interface from source. Updated `klp-components.json[header-phone].props`: renamed `breadcrumbSteps` → `breadcrumbs`, corrected type to `BreadCrumbStep[] | false`, added `showSearch` prop entry, fixed `onSearchClick` description. Fixed `dependencies.components` sort order to `["breadcrumbs", "button"]`. Added full Tokens section (7 layers). Replaced stub Examples section with actual 3-example content from `HeaderPhone.example.tsx`. Added second gap `root/literal-gap` to KLP:GAPS block (rgba fill with no token alias). Updated `docs/gaps.md`: Header Phone gap count 1→2, total 10→11. klp-doc-rules-validator: passed=true, autoFixed=[R1 inserted empty Props usage section — merged into existing API table], mismatches=[], warnings=[]. Reverse-index pass: graph symmetric — breadcrumbs.usedBy and button.usedBy already contain header-phone.

## [2026-04-21] DOCUMENT | header-desktop — 2 variants, 3 component dependencies (breadcrumbs, button, input), 0 usedBy
Regenerated `docs/components/_index_header-desktop.md`. Corrected stale API section: previous doc listed `breadcrumbSteps`, `onActionClick`, `onNewClick` (stale from component-adapter stub); replaced with actual `HeaderDesktopProps` interface from source (`title`, `features`, `actions`, `breadcrumbs`, `searchPlaceholder`, `onSearchChange`, `className`). Updated `klp-components.json[header-desktop].props` to match source. Added full Tokens section (5 layers × multiple properties each). Replaced stub Examples with actual `HeaderDesktop.example.tsx` content (3 examples). Added Anatomy detail with klpComponent annotations. klp-doc-rules-validator: passed=true, autoFixed=[R1 inserted Props usage section], mismatches=[], warnings=[]. Reverse-index pass: graph symmetric — breadcrumbs.usedBy, button.usedBy, input.usedBy all already contain header-desktop.

## [2026-08-04] DOCUMENT | input-multiselect — 5 variants, 7 dependencies (3 components, 3 externals, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_input-multiselect.md`. `klp-components.json` entry already present from component-adapter stub; added `doc` field, sorted `dependencies.components/externals/tokenGroups/brands` alphabetically. Synced `registry/input-multiselect.json#dependencies.npm` (already correct: `@radix-ui/react-popover`, `class-variance-authority`, `lucide-react` — no baseline-excluded packages present). Added Input Multiselect under `inputs` category in `docs/index.md`. Import scan: `@/components/input`, `@/components/badges`, `@/components/action-sheet-menu` detected — confirmed `dependencies.components: [action-sheet-menu, badges, input]`. Reverse-index pass: 3 new forward edges (input-multiselect→input, input-multiselect→badges, input-multiselect→action-sheet-menu). Patched `input` frontmatter `usedBy: [header-desktop, input-multiselect]`, `badges` frontmatter `usedBy: [input-multiselect, tabulation-cells]`, `action-sheet-menu` frontmatter `usedBy: [input-multiselect]` (was `[]`) and their `## Used by` sections. Patched `klp-components.json` usedBy for all three. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md` Used-by sections to add Input Multiselect. Patched `brands/klub.md` Used-by section to add Input Multiselect. Documented Class B blockquote above Variants table: `content` (derived from `value.length`) and `state` (mirrors Radix Popover open state) are spec variant axes with no corresponding prop on `InputMultiselectProps`. 2 composition gaps recorded (both `partial-reuse`): `chip-remove-icon` (Badge's `rightIcon` is decorative-only, wrapped in native `<button>` for the actual remove affordance) and `dropdown` (`ActionSheetMenu` hardcodes `role="menu"`, listbox semantics left uncorrected — out of scope for this adapter pass). Regenerated `docs/gaps.md` — total gaps: 13 → 15 across 7 → 8 components. klp-doc-rules-validator: passed=true, autoFixed=[], mismatches=[], warnings=[]. Reference screenshots unavailable this session (FIGMA_ACCESS_TOKEN expired at capture time) — flagged inline in the Variants section.

## [2026-08-04] DOCUMENT | calendar-button — 5 variants, 5 dependencies (1 external, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_calendar-button.md` (leaf component, no source doc existed previously). `klp-components.json` entry already present from component-adapter stub with `props` pre-populated; confirmed against source, refreshed `dependencies` (externals: `class-variance-authority`; tokenGroups: colors/radius/spacing/typography; brands: klub) and `updated` date. Registry `registry/calendar-button.json#dependencies.npm` already correct (`class-variance-authority` only — no baseline-excluded packages). Added Calendar Button under `inputs` category in `docs/index.md` (alphabetically after Button). Import scan: no `@/components/*` imports detected — leaf, `dependencies.components: []`. Documented Class B blockquote above Variants table: `state` is an exported prop (`CalendarButtonState`) but is `computed`, derived from `disabled`/`selected`/`today`/`otherMonth` in priority order — no 1:1 author-facing enum backs the cva state axis. Anatomy notes the Figma `Left Icon`/`Right Icon` slots are `visible:false` in all 5 states and are intentionally excluded from markup. Reference screenshots unavailable this session (expired Figma REST token — `screenshotNote` on every variant) — flagged inline in the Variants section with `> ❓ UNVERIFIED`. 0 composition gaps (leaf, `spec.composition.reuses: []`). Patched `tokens/colors.md`, `tokens/radius.md`, `tokens/spacing.md`, `tokens/typography.md`, and `brands/klub.md` Used-by sections to add Calendar Button. Reverse-index pass: no forward edges to propagate (leaf component); `usedBy: []` — the upcoming `calendar` panel component is expected to add this edge on its own DOCUMENT pass. Regenerated `docs/gaps.md` header timestamp — gap totals unchanged (15 across 8 components). klp-doc-rules-validator: passed=true, autoFixed=[R3 removed extraneous Do/Don't block — no props qualify as computed/persistent per validator rule], mismatches=[], warnings=[].

## [2026-08-04] DOCUMENT | calendar — 1 variant, 7 dependencies (3 components, 2 externals, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_calendar.md`. Figma master is a plain `COMPONENT` (no variant axes) — documented plainly, no variant matrix rendered; Variants section explains the dynamic 5/6-row week grid instead. `klp-components.json` entry already present from component-adapter stub; corrected `dependencies` shape to the canonical `{components, externals, tokenGroups, brands}` form (stub used a non-canonical `{components, npm}` shape) — `components: [button, calendar-button, input]`, `externals: [class-variance-authority, lucide-react]`, `tokenGroups: [colors, radius, spacing, typography]`, `brands: [klub]`. Registry `registry/calendar.json#dependencies.npm` already correct (`class-variance-authority`, `lucide-react`). Added Calendar under `inputs` category in `docs/index.md` (alphabetically before Calendar Button). Import scan: `@/components/button`, `@/components/calendar-button`, `@/components/input` detected — confirmed `dependencies.components: [button, calendar-button, input]`. Reverse-index pass: 3 new forward edges (calendar→button, calendar→calendar-button, calendar→input). Patched `button` frontmatter `usedBy: [calendar, collapsible, floating-alert, header-desktop, header-phone, list, list-content, modal-variation, pagination, sidebar, text-area]`, `calendar-button` frontmatter `usedBy: [calendar]` (was `[]` — first inbound edge), `input` frontmatter `usedBy: [calendar, header-desktop, input-multiselect]`, and their `## Used by` sections. Patched `klp-components.json` usedBy for all three. Patched `tokens/colors.md`, `tokens/spacing.md`, `tokens/radius.md`, `tokens/typography.md` Used-by sections to add Calendar. Patched `brands/klub.md` Used-by section to add Calendar. Documented an honest a11y `## Limitations` callout: the day grid uses `role="grid"` with native tab-order button focus, not roving-tabindex keyboard navigation — Arrow/PageUp/PageDown from the Figma spec's target keyboard support are not yet implemented. 1 composition gap recorded (`new-primitive` on `separator` — no klp Separator component exists; rendered as a local `separatorVariants` cva, same class of gap as ActionSheet Menu's separator). Reference screenshots unavailable this session (expired Figma REST token, `screenshotNote` on the single captured variant) — flagged inline in the Variants section with `> ❓ UNVERIFIED`. Regenerated `docs/gaps.md` — total gaps: 15 → 16 across 8 → 9 components. klp-doc-rules-validator: passed=true, autoFixed=[], mismatches=[], warnings=[].

## [2026-08-04] DOCUMENT | calendar — 1 variant, 7 dependencies (3 components, 2 externals, 4 token groups, 1 brand), 0 usedBy
Refreshed `docs/components/_index_calendar.md` — source API change since last pass: `CalendarProps` now extends `Omit<React.HTMLAttributes<HTMLDivElement>, 'defaultValue'>` and the root `<div>` spreads `{...props}` after a default `role="application"` that a host can override. Updated the Props usage "Extends" line accordingly. Documented the dual-usage contract (standalone block vs. `Popover.Content asChild` dropdown) in the component description and added a second Examples sub-section sourced from `playground/routes/calendar.tsx`'s `DatePickerDemo` (Input trigger + Popover.Anchor/Portal/Content asChild + Calendar, closing on select), noting the DS has no `date-picker` component yet — this composition is consumer-assembled. Import scan re-run: no new `@/components/*` or external imports (Popover is only used in the playground route, not in source) — `dependencies` unchanged (`components: [button, calendar-button, input]`, `externals: [class-variance-authority, lucide-react]`). Registry `registry/calendar.json#dependencies.npm` unchanged. Props table unchanged (no new named prop — the interface extension only widens the root's accepted native attributes). Anatomy, Tokens, the `new-primitive` separator gap, and the `role="grid"` keyboard-navigation limitation preserved verbatim. `KLP:NOTES` block preserved. Reverse-index pass: no forward-edge changes (dependencies unchanged) — no-op, idempotent. klp-doc-rules-validator: passed=true, autoFixed=[], mismatches=[], warnings=[].

## [2026-08-04] DOCUMENT | calendar — 1 variant, 7 dependencies (3 components, 2 externals, 4 token groups, 1 brand), 0 usedBy
Refreshed `docs/components/_index_calendar.md` — sizing-only change since last pass: `rootVariants` root width is now `w-[360px] min-w-[320px]` (was `w-[360px]`, still overridable via `cn`/tailwind-merge), `dayGridVariants` width is now `w-full` (was `w-[328px]`), and `weekdayHeaderVariants`/`weekRowVariants` dropped the fixed `gap-[13px]` in favor of `justify-between` alone. Updated the `root`, `day-grid`, `weekday-header`, and `week-row` Tokens tables: kept the Figma-captured literals (`360px`, `328px`, `13px`) as the table source of truth per source-of-truth hierarchy, and appended a blockquote to each documenting the fluid contract (360px standalone default, tracks host width when `className` overrides it, 320px floor — the geometric minimum for 7×36px day cells plus padding) and why the fixed gap would overflow at 320px (330px of cells+gaps in 288px available). Updated the "As an input's dropdown" Example to show `className="w-[var(--radix-popover-trigger-width)]"` on the `Calendar` inside `Popover.Content asChild`, and re-sourced it from `playground/routes/calendar.tsx`'s parameterized `DatePickerDemo({ width })`, now demonstrating a 480px field (panel follows to 480px) and a 240px field (panel floors at 320px) — both verified in the browser. Anatomy, Props usage, Variants, Accessibility, the `new-primitive` separator gap, the `role="grid"` keyboard-navigation limitation, and the dual-usage prose preserved verbatim. `KLP:NOTES` block preserved. Import scan re-run: no change (`dependencies` and `registry/calendar.json#dependencies.npm` unchanged). Reverse-index pass: no forward-edge changes — no-op, idempotent. `docs/gaps.md` unchanged (gap set unchanged). klp-doc-rules-validator: passed=true, autoFixed=[], mismatches=[], warnings=[].

## [2026-08-04] DOCUMENT | separator — 8 variants, 0 dependencies, 1 usedBy
Generated `docs/components/_index_separator.md` (new component, category `utilities` — first entry in that category, added `### utilities` section to `docs/index.md`). `klp-components.json` and `registry/separator.json` canonical entries were already present on disk (matching this pass's scan output exactly: `dependencies.components: []`, `externals: [@radix-ui/react-separator, class-variance-authority]`, `tokenGroups: [colors, spacing]`, `brands: [klub]`) — no changes needed to either. Import scan of `Separator.tsx`: no `@/components/*` imports (leaf), external imports `@radix-ui/react-separator` (newly added to `package.json`, v1.1.15) and `class-variance-authority`. `direction` maps onto Radix's `orientation` prop; `decorative` defaults to `true` so the rendered role is `none` rather than `separator` by default — documented under Props usage and Accessibility. Anatomy and Tokens sections trace the root/line split from `spec.json`: `root` is a padding-only transparent wrapper (margin axis → block-axis padding for horizontal, inline-axis padding for vertical; `none` has zero padding), `line` is a `border-*` stroke never a `bg-*` fill, matching the Figma capture where the rule node has a `strokes` entry and no `fills` entry. Documented that the 234px length seen in every Figma variant is a canvas sample only — the line fills its container (`w-full`/`h-full`) in the real implementation, verified in the browser at all 8 variants (wrapper measures 1/17/33/49px on the margin axis, line resolves to `#D7DAD9` under klub). Reference screenshots unavailable this session (expired Figma REST token, `screenshotNote` on every one of the 8 variants) — flagged inline in the Variants section with `> ❓ UNVERIFIED`; the Variants table therefore shows `—` in every cell pending re-capture. 0 composition gaps (`spec.composition.reuses: []`, leaf component). Patched `tokens/colors.md` and `tokens/spacing.md` Used-by sections to add Separator; patched `brands/klub.md` Used-by section to add Separator. klp-doc-rules-validator: passed=true, autoFixed=[], mismatches=[], warnings=[]. `node scripts/validate-tokens.mjs separator`: 0 mismatches, 0 warnings. Reverse-index pass: 1 forward edge from `calendar` (already importing `@/components/separator` per its own SYNC pass in this run) — `usedBy: [calendar]` set directly from that edge, no propagation needed elsewhere.

## [2026-08-04] SYNC | calendar — gap resolution (separator now reused, not inlined)
Refreshed `docs/components/_index_calendar.md` following the component-adapter's migration of the footer rule from a local `separatorVariants` cva to the newly-adapted `@/components/separator`. `klp-components.json[calendar].dependencies.components` and `registry/calendar.json#dependencies.components` were already updated by the adapter to include `separator` (confirmed against a fresh import scan of `Calendar.tsx`: `@/components/button`, `@/components/calendar-button`, `@/components/input`, `@/components/separator` — no drift). `registry/calendar.json`'s `composition.reuses[]` now lists `separator` (`direction="horizontal" margin="none"`) and `composition.gaps[]` is empty — previously carried the `new-primitive`/`separator` gap. Updated the doc page's frontmatter `dependencies.components` to `[button, calendar-button, input, separator]`; updated the Anatomy tree's `separator` line from `GAP: new-primitive` to `REUSED: separator`; updated the `separator` layer's Tokens sub-section to note it is now fully owned by the reused Separator component instead of a local cva; added Separator to the Dependencies → klp components list. Cleared the `KLP:GAPS` block — now reads "No gaps recorded." (previously listed the `new-primitive`/`separator` gap). `node scripts/validate-tokens.mjs calendar`: 0 mismatches, 0 warnings — the previous `undeclared-reuse` warning under the `reuse` check family is gone now that `spec.composition.reuses[]` in `.klp/figma-refs/calendar/spec.json` includes `separator` with `anatomy[].klpComponent` set on the separator part. Props, Variants, Examples, Accessibility, and `KLP:NOTES` preserved verbatim — no API change. Regenerated `docs/gaps.md`: removed the Calendar row (total gaps 16 → 15, components-with-gaps 9 → 8); `calendar` no longer appears in the report. Reverse-index pass: 1 new forward edge `calendar → separator`; patched `separator`'s frontmatter/`## Used by` to `[calendar]` (see the separator DOCUMENT entry above — done together in this pass) and `klp-components.json[separator].usedBy`. `button`, `calendar-button`, `input` edges unchanged (no removals — all three are still imported). klp-doc-rules-validator: passed=true, autoFixed=[], mismatches=[], warnings=[].

## [2026-08-05] DOCUMENT | nav-item — 2 variants, 4 dependencies (1 component, 2 externals, 3 token groups, 1 brand), 1 usedBy
Generated `docs/components/_index_nav-item.md` (new component, category `navigation`, brand `showup`). Import scan of `NavItem.tsx`: `@radix-ui/react-slot`, `class-variance-authority` externals only — `lucide-react` is NOT imported in source (icon is a consumer-passed `ReactNode` slot) despite the adapter stub and `registry/nav-item.json` listing it; corrected both `klp-components.json[nav-item].dependencies.externals` and `registry/nav-item.json#dependencies.npm` to drop it. `@/components/badges` detected — confirmed `dependencies.components: [badges]`. Documented plainly, per explicit instruction, that `default` vs `active` differ by nothing but a bottom-only 2px underline (`border-transparent` → `border-klp-border-light`) — text color, icon color, padding, and layout are byte-identical between the two Figma variants. Flagged the component as not tokenized in Figma: every color and the Arial/Bold/14 label type are hardcoded literals with zero bound variables; the klp mapping (`--klp-fg-on-emphasis`, `--klp-border-light`, label typography tokens) is a reconciliation verified by exact hex/value match, not a Figma binding. Recorded 3 typed gaps in `KLP:GAPS` (`label-typography` no-instance-no-match, `content-underline-color` token-gap, `content-padding-y` token-gap) sourced from the fuller gap detail supplied for this pass (the `klp-components.json` stub only carried 1 of the 3). Documented the `h-[71px]`-instead-of-`paddingY:25px` deviation as an implementation note (not a gap) — the padded box overflowed the 80px header with the counter Badge present. Reference screenshots unavailable this session (expired Figma REST token, `screenshotNote` on both variants) — flagged `❓ UNVERIFIED` in the Variants table. `node scripts/validate-tokens.mjs nav-item`: 0 mismatches, 6 informational warnings (`unknown-state` on the Slot/button root, `layer-no-cva` on `icon`/`label`/root) — expected for this component's shape, noted in `## Notes`. Enriched `klp-components.json[nav-item].props[*].description` from source JSDoc (adapter stub left them empty). klp-doc-rules-validator: passed=true, autoFixed=[R4 inserted Class B blockquote above `## Variants`], mismatches=[], warnings=[].


## [2026-08-05] DOCUMENT | header-showup — 1 variant, 9 dependencies (3 components, 2 externals, 4 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_header-showup.md` (new component, category `navigation`, brand `showup`). Figma master is a plain `COMPONENT` with no variant axes (14 anatomy layers) — documented plainly, no variant matrix rendered. Import scan of `HeaderShowup.tsx`: `class-variance-authority`, `lucide-react` externals (`ChevronDown`); `@/components/badges`, `@/components/button`, `@/components/nav-item` — confirmed `dependencies.components: [badges, button, nav-item]`, matching the already-correct `klp-components.json`/`registry/header-showup.json` stub written by the adapter. Documented the three zones (Logo/grow, Items/hug+bottom-aligned, Right/grow+end-justified) and the bottom-alignment rationale (lets each nav-item's active underline meet the header's own bottom edge). Recorded the 4 hidden Right-zone children (search icon, second Button, bell-with-dot group, user icon) as a Anatomy callout — dead Figma layers, no markup emitted. Recorded 3 `new-primitive` gaps in `KLP:GAPS` (`logo-mark`, `user-avatar`, `language-selector`) — all deliberate consumer-provided slots (`logo`, `userSlot`, `languageSelector`), each documented alongside its default rendering and default token bindings. Flagged a `⚠️ CONTRADICTION` on the `root` fill: `spec.json` captures the Figma literal as `#141B4D` (= `--klp-color-midnight-900`), but the bound variable name (`bg/brand-contrasted`) resolves under the current `aliases.css` to `--klp-color-midnight-800` (`#202A64`) — flagged for human review, not corrected. Reference screenshot unavailable this session (expired Figma REST token) — flagged `❓ UNVERIFIED` in Variants. Reverse-index pass: 3 new forward edges (`header-showup→badges`, `header-showup→button`, `header-showup→nav-item`, the latter documented in this same pass). Patched `badges` frontmatter/`## Used by` to `[header-showup, input-multiselect, nav-item, tabulation-cells]`; patched `nav-item` frontmatter/`## Used by` to `[header-showup]` (first inbound edge); also corrected a pre-existing drift on `button` — `klp-components.json[button].usedBy` already listed `header-showup` but `docs/components/_index_button.md` did not, now fixed on both frontmatter and `## Used by`. Patched `klp-components.json` usedBy for `badges`/`button`/`nav-item`. Patched `tokens/{colors,radius,spacing,typography}.md` Used-by sections to add both Nav Item and Header Showup; patched `brands/showup.md` Used-by section (was empty — first two showup-brand components). Regenerated `docs/gaps.md` — total gaps: 14 → 20 across 7 → 9 components. klp-doc-rules-validator: passed=true, autoFixed=[], mismatches=[], warnings=[].

## [2026-08-05] SYNC | nav-item, header-showup — post-capture design/token fixes, 2 components, 0 new forward edges
Resynced both after source/token changes outside the pipeline (specs unchanged). **nav-item**: documented the newly-added `hover-overlay` layer — an absolutely-positioned `span` (`pointer-events-none`, inset `8px` top/bottom → 64px tall, `-8px` left/right past the label, `rounded-klp-l`, `bg-klp-alpha-10`, `opacity-0 → group-hover:opacity-100`) — as a **design instruction implemented outside the captured spec**: the Figma component set only declares `State: Default | Active`, no hover appearance. Added an Anatomy callout, a "Hover (design addition, not a captured variant)" note under Variants, a new `hover-overlay` Tokens sub-section, and a 4th typed gap (`token-gap`/`hover-overlay`) recording that a new alias, `--klp-alpha-10 → --klp-color-light-10`, had to be added to `aliases.css` in all four `[data-brand]` blocks since Figma's alpha family only defined `alpha/80` — a DS-side addition, not a Figma capture; flagged for design to add a matching `alpha/10` alias. `NavItem.tsx`'s now-exported `hoverOverlayClasses` confirmed already listed alongside the other exports — no doc change needed there. Appended a dated entry to `KLP:NOTES` recording the implementation (preserved existing prose verbatim). Updated `klp-components.json[nav-item].composition.gaps` (+1 entry) and `updated` date; no dependency/registry change (no new npm import). **header-showup**: removed the stale `⚠️ CONTRADICTION` on the `root` fill — a live comparison of all 124 alias × brand pairs between Figma and the repo found 122 exact matches and 2 stale `.klp/tokens.json` entries (dated 2026-04-16), now corrected and re-synced: `bg-brand-contrasted`/showup `midnight-800→midnight-900`, `bg-secondary-brand-contrasted`/atlas `fuchsia-800→fuchsia-700`. `--klp-bg-brand-contrasted` now resolves to `#141B4D` under showup, an exact match to the Figma capture (verified in the browser). Corrected the `root` `fill` and `user-avatar` `initials color` "Resolved (showup)" cells from `#202A64` to `#141B4D`; appended a dated `KLP:NOTES` entry recording the resolution and the corrected alias pairs (existing prose preserved verbatim). No dependency/gap/registry change — the 3 pre-existing `new-primitive` gaps are untouched. Added a standing note to `docs/tokens/_index_tokens.md` (`KLP:NOTES`, preserved existing empty block) and a new "Alpha" section to `docs/tokens/colors.md` (plus its `Used by` bullet for Nav Item) recording that `.klp/tokens.json` is now a **partial re-capture** — the 3 entries above (2 corrections + the new `alpha-10` alias) are live-verified as of 2026-08-05, the remaining 121 alias × brand pairs are still the 2026-04-16 capture; this split is not tracked anywhere machine-readable today. Reverse-index pass: graph re-verified symmetric — no forward-edge changes on either component (`nav-item.dependencies.components=[badges]`, `header-showup.dependencies.components=[badges,button,nav-item]` both unchanged), `badges.usedBy` and `button.usedBy` already correct from the prior DOCUMENT passes. Regenerated `docs/gaps.md`: Nav Item gap count 3→4, total 20→21 across 9 components. klp-doc-rules-validator: passed=true for both components, autoFixed=[], mismatches=[], warnings=[].

## [2026-08-06] DOCUMENT | navbar-item — 3 variants, 1 dependency (1 externals, 3 token groups, 1 brand), 1 usedBy
Generated `docs/components/_index_navbar-item.md` (new component, category `navigation`, brand `atlas`). Import scan of `NavbarItem.tsx`: no `@/components/*` imports (leaf), external import `class-variance-authority` only — corrected `klp-components.json[navbar-item].dependencies.externals` and `registry/navbar-item.json#dependencies.npm` from the adapter's stale stub (`["@radix-ui/react-slot","class-variance-authority","lucide-react"]`, neither Slot nor lucide-react is imported in source; the icon is a consumer-passed `ReactNode`). Documented plainly, per explicit instruction, that the three `state` variants (`default`/`hover`/`selected`) differ ONLY on the root surface — icon colour, label colour, type, and geometry are byte-identical across all three (source: `NavbarItem.tsx`, only `rootVariants` branches on `state`). Contrasted explicitly against [Nav Item](./components/_index_nav-item.md): unlike that ShowUp sibling, `navbar-item` is fully and correctly tokenized in Figma — every layer resolves to a bound variable with a matching `--klp-*` alias (source: spec.json:description). Documented `state` as a `computed` prop (derives from real `:hover`, forceable statically) with `selected` (author-supplied, always wins over hover and a forced `state`) as `optional` — flagged as a Class B–ish situation per the `@propClass`/`@derivedFrom` JSDoc tags. Reference screenshots unavailable this session (expired Figma REST token) — flagged `❓ UNVERIFIED` in the Variants table. Flagged a minor spec inconsistency on the root layer's `--klp-size-4xs` padding token (`figmaVar: null` yet a captured raw value of 4px vs. the token's actual 2px resolution) as a `❓` footnote, not a typed gap. 0 composition gaps (`spec.composition.reuses: []`, leaf component) — `KLP:GAPS` reads "No gaps recorded." `radixPrimitive` set to `null` in frontmatter/registry/klp-components.json — the source never imports `@radix-ui/react-slot` despite spec.json's stale `"@radix-ui/react-slot"` TODO guess. `klp-doc-rules-validator`: passed=true, autoFixed=[], mismatches=[], warnings=[]. `node scripts/validate-tokens.mjs navbar-item`: 0 mismatches. Reverse-index pass: 0 forward edges (leaf) — no propagation from this component; the inbound edge from `sidebar-atlas` is set directly below (documented together in this same pass). Patched `tokens/{colors,spacing,typography}.md` Used-by sections; patched `brands/atlas.md` Used-by section (was previously only `button`).

## [2026-08-06] DOCUMENT | sidebar-atlas — 1 variant, 2 dependencies (1 component, 1 external, 2 token groups, 1 brand), 0 usedBy
Generated `docs/components/_index_sidebar-atlas.md` (new component, category `navigation`, brand `atlas`). Figma master is a plain `COMPONENT` with no variant axes — documented plainly, no variant matrix rendered; Figma models interaction states one level down inside each `navbar-item` instance. Import scan of `SidebarAtlas.tsx`: `class-variance-authority` external only (no `lucide-react` import in source — icons are consumer-passed via `items[].icon`); `@/components/navbar-item` detected — confirmed `dependencies.components: [navbar-item]`, correcting the adapter stub's `dependencies.externals` (dropped `lucide-react`, matching the actual import scan) in both `klp-components.json` and `registry/sidebar-atlas.json#dependencies.npm`. Documented the fixed 70px-wide, full-height rail geometry (24px padY, 16px gap, no wrapper frame around items) and the ten real captured entries (Search, Map, Dashboard, Todo, Benchmark, Matching, Operations, Contracts, Reporting, Admin), calling out per explicit instruction that the third entry's Figma instance is layer-named "Home" but its rendered TEXT copy reads "Dashboard" — the component correctly uses the rendered label ("Dashboard"), not the layer name. Recorded exactly 2 typed gaps in `KLP:GAPS`: `logo` (`new-primitive` — Figma IMAGE fill, no klp equivalent, exposed as a `logo` slot with a minimal placeholder default) and `root` (`no-instance-no-match`/`token-gap` — fill `#22222D` bound to no Figma variable anywhere in the file; DS-side alias `--klp-bg-navrail → storm-900` created, RGB distance 10, flagged as a genuine Figma-side gap needing a real token). Dropped a stale third `partial-reuse` gap (`nav-item-*`) from the adapter's stub describing the now-fixed `asChild` runtime crash — resolved as of this pass, no longer a gap (see the SYNC entries below and Notes on this page). `radixPrimitive` corrected to `null` in frontmatter/klp-components.json — the shipped implementation uses a bare `<nav>`, not `spec.json`'s TODO best-guess of `@radix-ui/react-navigation-menu`; documented the reasoning in Notes. Reference screenshot unavailable this session (expired Figma REST token) — flagged `❓ UNVERIFIED` in Variants. `klp-doc-rules-validator`: passed=true, autoFixed=[], mismatches=[], warnings=[]. `node scripts/validate-tokens.mjs sidebar-atlas`: 0 mismatches, 0 warnings. Reverse-index pass: 1 new forward edge `sidebar-atlas→navbar-item`; patched `navbar-item`'s frontmatter/`## Used by` to `[sidebar-atlas]` and `klp-components.json[navbar-item].usedBy`. Added the `### navigation` entries for both `navbar-item` and `sidebar-atlas` to `docs/index.md`. Regenerated `docs/gaps.md`: +1 component (Sidebar (Atlas), 2 gaps), total gaps 21 → 23 across 9 → 10 components.

## [2026-08-06] SYNC | nav-item, navbar-item, header-showup, sidebar-atlas — asChild removed, href added (runtime crash fix)
Documented a runtime bug fix across the navigation family, already applied in source outside this pass. **Root cause**: `nav-item` and `navbar-item` both previously exposed Radix `asChild` for rendering an `<a href>`. That path crashed at runtime — verified in a browser probe, it threw `React.Children.only expected to receive a single React element child` and rendered a blank page. Two compounding reasons: both components render several sibling children under the `Slot` (a `hover-overlay` span + `content` box on `nav-item`; an `icon` span + `label` span on `navbar-item`), and `Slot` requires exactly one child; more fundamentally, Radix `Slot` clones the consumer's element and keeps *its* children, which would have silently discarded each component's own icon/label/overlay structure regardless of the sibling-count issue. **Fix**: `asChild` is removed from both `nav-item` and `navbar-item`, replaced by a plain `href` prop — set it and the root renders as `<a href>` with all internal structure intact; omit it and you get a `<button type="button">`. `header-showup` (`items[].href`) and `sidebar-atlas` (`items[].href`) forward onto this prop unchanged — verified in the browser: all three href paths (`nav-item` standalone, `navbar-item` standalone, and both composites forwarding through) now render a real `<a>` with icon/label/overlay intact, and the non-href paths still render buttons. Updated Props tables on all 4 doc pages (`asChild` gone, `href` added on `nav-item`/`navbar-item`; `header-showup`'s `items[].href` description corrected from "via nav-item's asChild/Slot pattern" to "via nav-item's href prop"; `sidebar-atlas` documented fresh with `href` from the start, see the DOCUMENT entry above). Rewrote the Examples code fence on `nav-item`'s doc page to drop `asChild`/`<a>`-as-child usage in favor of `href="..."` directly on `NavItem` (matching the already-rewritten `.example.tsx`). Fixed `nav-item`'s Accessibility prose (was "pass `asChild` and render an `<a href>`", now "pass `href` to render an `<a href>`"). The `@radix-ui/react-slot` dependency is no longer imported by either component — corrected `dependencies.externals`/`radixPrimitive` (now `null`) in `klp-components.json` for both `nav-item` and `navbar-item`, and `dependencies.npm`/`meta.radixPrimitive` in `registry/nav-item.json` and `registry/navbar-item.json`. `header-showup` and `sidebar-atlas` never imported `@radix-ui/react-slot` directly — no dependency change on either. Appended dated `KLP:NOTES` entries to `nav-item`, `header-showup`, and `sidebar-atlas` recording the fix (existing prose preserved verbatim on all three); `navbar-item`'s Notes (new page) documents it directly. `klp-doc-rules-validator --fix`: passed=true for all 4 components, autoFixed=[], mismatches=[], warnings=[]. `node scripts/validate-tokens.mjs <name>`: 0 mismatches for all 4 (informational-only warnings on `nav-item`/`navbar-item`, from `layer-no-cva` and `unknown-state` against spec.json's stale `radixPrimitive` field — expected, unrelated to this fix). Reverse-index pass: graph re-verified symmetric across all 4 — `nav-item.usedBy=[header-showup]`, `navbar-item.usedBy=[sidebar-atlas]`, `header-showup.dependencies.components=[badges,button,nav-item]`, `sidebar-atlas.dependencies.components=[navbar-item]`, no changes needed (no forward-edge changes — component/token-group/brand dependency sets are unaffected by this API surface change). `docs/gaps.md` unaffected by the SYNC portion of this pass (gap changes are attributable to the sidebar-atlas DOCUMENT entry above, not this fix).
