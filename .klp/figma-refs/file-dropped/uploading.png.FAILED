FAILED: could not persist screenshot to disk.

Same root cause as default.png.FAILED (REST token expired; plugin-bridge
screenshot tool has no disk-write path). Additionally, isolated capture of
node 113648:1330 (lucide/loader-circle) rendered as a blank/near-empty image
even via the plugin bridge — this node sits outside the visible clip bounds of
its parent "Icons Rotation" frame in the current static Figma state (see
spec.json anatomy[].icon-highlight notes), so there is no true visual
reference for the "uploading" state to capture. The `uploading` variant in
spec.json is synthesized per the task brief (loader-circle icon, spin
animation left to the adapter — no Figma motion spec exists for it).
